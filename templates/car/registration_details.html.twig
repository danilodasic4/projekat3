<!DOCTYPE html>
<html>
<head>
    <title>Car Registration Details</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    {% include 'base.html.twig' %}
    
    <div class="container mt-5">
        <!-- Header -->
        <div class="text-center mb-4">
            <h1 class="display-5">Registration Details</h1>
            <p class="lead">Get the details and cost of registering your car.</p>
        </div>

        <!-- Car Details -->
        <div class="card mb-4 shadow-sm">
            <div class="card-body">
                <h2 class="card-title">{{ car.brand }} {{ car.model }}</h2>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item"><strong>Year:</strong> {{ car.year }}</li>
                    <li class="list-group-item"><strong>Engine Capacity:</strong> {{ car.engineCapacity }}cc</li>
                    <li class="list-group-item"><strong>Color:</strong> {{ car.color }}</li>
                </ul>
            </div>
        </div>

        <!-- Cost Section -->
        <div class="alert alert-info text-center">
            <h3>Registration Cost: <strong>{{ baseCost }} RSD</strong></h3>
        </div>

        <!-- Discount Form -->
        <div class="card mb-4 shadow-sm">
            <div class="card-body">
                <form method="POST">
                    <div class="form-group">
                        <label for="discountCode" class="form-label">Discount Code</label>
                        <input type="text" name="discountCode" id="discountCode" class="form-control" placeholder="Enter discount code">
                    </div>
                    <button type="submit" class="btn btn-primary btn-block mt-3">Apply Discount</button>
                </form>

                {% if finalCost is not same as(baseCost) %}
                <div class="alert alert-success mt-4">
                    <h4>Final Cost with Discount: <strong>{{ finalCost }} RSD</strong></h4>
                </div>
                {% endif %}
            </div>
        </div>

        <!-- Back Button -->
        <div class="text-center">
            <a href="{{ path('app_car_index') }}" class="btn btn-secondary">Back to Car List</a>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.0/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
