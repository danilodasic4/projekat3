<!DOCTYPE html>
<html>
<head>
    <title>Car Registration Details</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    {% include 'base.html.twig' %}
    
    <div class="container mt-4">
        <h1>Registration Details for {{ car.brand }} {{ car.model }}</h1>
        <p><strong>Year:</strong> {{ car.year }}</p>
        <p><strong>Engine Capacity:</strong> {{ car.engineCapacity }}cc</p>
        <p><strong>Color:</strong> {{ car.color }}</p>

        <h3>Registration Cost: {{ baseCost }} EUR</h3>

        <form method="POST">
            <div class="form-group">
                <label for="discountCode">Discount Code</label>
                <input type="text" name="discountCode" id="discountCode" class="form-control" placeholder="Enter discount code" />
            </div>
            <button type="submit" class="btn btn-primary">Apply Discount</button>
        </form>

        {% if finalCost is not same as(baseCost) %}
        <h3 class="mt-3">Final Cost with Discount: {{ finalCost }} RSD</h3>
        {% endif %}
        
        <a href="{{ path('app_car_index') }}" class="btn btn-secondary mt-3">Back to Car List</a>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.0/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
