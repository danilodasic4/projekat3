
<h1>Edit Car</h1>

{{ form_start(form, { 'action': path('app_car_edit', { 'id': car.id }), 'method': 'POST' }) }}
    <input type="hidden" name="_method" value="PUT">
    {{ form_row(form.brand) }}
    {{ form_row(form.model) }}
    {{ form_row(form.year) }}
    {{ form_row(form.engineCapacity) }}
    {{ form_row(form.horsePower) }}
    {{ form_row(form.color) }}
    {{ form_row(form.registrationDate) }}
    {{ form_row(form.save) }}
{{ form_end(form) }}
